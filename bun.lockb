#!/usr/bin/env bun
bun-lockfile-format-v0

# VisionStack Bun placeholder lockfile
# Ensures Bun recognizes this project in Lovable
# Actual dependencies are managed via package.json / package-lock.json
